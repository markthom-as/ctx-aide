interface:
  display_name: "CTX Aide"
  short_description: "Plan and harden repo-local agent tickets."
  default_prompt: "Use the ctx-aide skill to turn this request into a hardened spec, ticket pack, or implementation-ready ticket."
policy:
  allow_implicit_invocation: true
